# Required — set these in Railway's Variables tab (or your hosting provider)
APP_PASSWORD=changeme
AUTH_SECRET=change-this-to-a-long-random-string

# Optional — defaults shown above are used if not set
# PORT is set automatically by Railway; Next.js picks it up without any config
